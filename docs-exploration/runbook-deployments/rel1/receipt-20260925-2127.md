PLANNED

## Instance Information
- **Instance ID:** rel1
- **Runbook:** docs-exploration/runbooks/deploy-rel.md
- **Resource Prefix:** `or-rel1`
- **Target GCP Project:** `barni-cnrm-20260529`
- **Region / Zone:** `us-central1` / `us-central1-a`

## Resources Planned
- **GKE Cluster:** `or-rel1-gke-rel` (1x `e2-standard-4` control/system node, GcpFilestoreCsiDriver enabled, labeled `repo-agent-instance=or-rel1`)
- **GKE GPU Node Pool:** `gpu-dra` on `or-rel1-gke-rel` (1x `g2-standard-24` with 2x NVIDIA L4 GPUs, DRA labels and taints, 200GB boot disk)
- **GCP Filestore:** 1TiB Standard Filestore instance dynamically provisioned via `standard-rwx` StorageClass / PVC `open-rl-shared-pvc` in `openrl-system`
- **Kubernetes Workloads & Drivers:**
  - NVIDIA Preloaded Host Driver DaemonSet (`cos/daemonset-preloaded-latest.yaml`)
  - NVIDIA DRA GPU Driver DaemonSet & Controller (`nvidia-dra-driver-gpu` v25.8.0 Helm chart)
  - OpenRL Release Workloads: `redis-store`, `open-rl-scheduler`, `open-rl-api-server` deployed from official release manifest (`openrl-lora.yaml` pinned to `ghcr.io/gke-labs/open-rl/*`)

## Estimated Costs
- **G2 GPU VM (`g2-standard-24` with 2x L4):** ~$1.68/hr
- **E2 System Node (`e2-standard-4`):** ~$0.13/hr
- **GKE Cluster Management Fee:** $0.10/hr
- **Filestore Standard (1 TiB):** ~$0.20/hr (~$144/month pro-rated)
- **Total Estimated Cost:** ~$2.11/hr during active execution. Teardown completely removes cluster and storage.

## Verified Feasibility Checklist
- [x] GCP IAM permissions for cluster and node pool management (`roles/container.admin`)
- [x] GCP IAM permissions for Filestore instance creation (`roles/file.editor` / `file.googleapis.com`)
- [x] GCP IAM service account user permission (`roles/iam.serviceAccountUser`)
- [x] `gcloud` CLI (Google Cloud SDK 586.0.0)
- [x] `kubectl` CLI (v1.35.8-dispatcher with server-side apply support)
- [x] `curl` CLI (v8.14.1)
- [x] `gh` CLI (v2.82.1)
- [ ] `helm` CLI v3 (for NVIDIA DRA driver installation) — ✗ MISSING: `curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash`
- [ ] `uv` CLI (optional for running host test clients) — ✗ MISSING: `curl -LsSf https://astral.sh/uv/install.sh | sh`

## Needs from Owner
- Install `helm` CLI (`curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash`) before triggering execution so that step 4 can install `nvidia-dra-driver-gpu`.

## Deviations from the Runbook
- Cluster creation in `deploy.sh` adds `--labels="repo-agent-instance=or-rel1"` to ensure instance ownership traceability on GCP cloud resources.
- `teardown.sh` wraps `kubectl get-credentials` and `kubectl delete` with defensive fallback so that cluster deletion proceeds cleanly even if credentials or workloads were not previously configured.

## Notes for Reviewer
Review `params.env` and `deploy.sh` before clicking Deploy. No infrastructure has been created or modified during this planning phase.
