PLANNED

## Instance Information
- **Instance Identifier:** `instance1`
- **Resource Prefix:** `or-instance1`
- **GCP Project:** `barni-cnrm-20260529`
- **Region / Zone:** `us-central1` / `us-central1-a`
- **GKE Cluster Name:** `or-instance1-gke-dra`
- **GPU Node Pool Name:** `gpu-dra` (1 node of `g2-standard-24` with 2x NVIDIA L4 GPUs)
- **Default Node Pool:** 1 node of `e2-standard-4`
- **Storage:** Google Cloud Filestore 1TiB instance provisioned via GKE Filestore CSI driver (`standard-rwx` PVC `open-rl-shared-pvc`)

## Estimated Cost
- **GKE Control Plane:** ~$0.10/hour (~$73/month)
- **Primary Node Pool (`e2-standard-4`):** ~$0.134/hour
- **GPU DRA Node Pool (`g2-standard-24` with 2x L4 GPUs):** ~$1.68/hour
- **Persistent Boot Disks (100GB + 200GB pd-balanced):** ~$0.03/hour
- **Filestore Shared Volume (1TiB Basic SSD/Standard):** ~$0.20 - $0.30/hour
- **Total Estimated Burn Rate:** ~$2.15 - $2.25/hour while running

## Feasibility State
- [x] GCP IAM permissions for cluster and node pool management (`roles/container.admin`)
- [x] GCP IAM permissions for Filestore instance creation (`roles/file.editor` / `file.googleapis.com`)
- [x] GCP IAM service account user permission (`roles/iam.serviceAccountUser`)
- [x] `gcloud` CLI (Google Cloud SDK 586.0.0 installed)
- [x] `kubectl` CLI (v1.35.8 with Kustomize v5.7.1 installed)
- [ ] `helm` CLI v3 — ✗ MISSING: `curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash`
- [ ] `uv` CLI (for optional client verification tests) — ✗ MISSING: `curl -LsSf https://astral.sh/uv/install.sh | sh`

## Needs from Owner
- Install `helm` CLI v3 in the execution environment prior to running `deploy.sh` so the NVIDIA DRA GPU driver helm chart (`nvidia-dra-driver-gpu` v25.8.0) can be installed.
- (Optional) Install `uv` if running end-to-end Python test client scripts from `examples/`.

## Deviations from the Runbook
- None. All steps in `deploy.sh` and `teardown.sh` directly mirror `docs-exploration/runbooks/deploy-gke.md` using the resolved instance parameters.

## Pre-Deployment Notes for Owner
- Review `params.env` to verify GCP project (`barni-cnrm-20260529`) and region (`us-central1`).
- Ensure G2 GPU quota (`NVIDIA_L4_GPUS` >= 2) is available in `us-central1-a`.
- Once `helm` is installed, `deploy.sh` will create the cluster, provision the GPU pool with DRA labels and taints, install NVIDIA drivers, and apply the OpenRL LoRA control plane.
